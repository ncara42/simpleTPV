import { describe, expect, it } from 'vitest';

import { PromotionsModule } from './promotions.module.js';

describe('PromotionsModule', () => {
  it('queda definido', () => {
    expect(PromotionsModule).toBeDefined();
  });
});
