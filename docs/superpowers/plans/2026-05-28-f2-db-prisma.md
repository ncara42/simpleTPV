# F2 — packages/db Prisma + Postgres Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implantar `packages/db` con schema Prisma mínimo (5 modelos), aislamiento multi-tenant vía RLS, seed con 2 organizaciones, y Postgres 16 levantado vía docker-compose.

**Architecture:** Postgres 16-alpine en docker-compose con bind 127.0.0.1. Schema Prisma en `packages/db/prisma/schema.prisma` con Organization/Store/User/UserStore/Product. Dos migraciones: `initial` (autogenerada) y `add_rls` (manual con roles `app`/`app_admin`, `FORCE ROW LEVEL SECURITY`, policy por tabla usando `current_setting('app.current_organization_id', true)::uuid`). Cliente Prisma generado a `packages/db/generated/client/`, gitignored.

**Tech Stack:** PostgreSQL 16, Docker Compose, Prisma 6, @prisma/client 6, bcryptjs 3, tsx 4, TypeScript 6.

**Spec de referencia:** `docs/superpowers/specs/2026-05-28-f2-db-prisma-design.md`

---

## Convenciones del plan

- **Rutas:** todas absolutas a `/Users/admin/Desktop/simpletpv/`.
- **Commits:** Conventional Commits, uno por tarea.
- **Verificación:** cada tarea valida el resultado antes de commitear.
- **Postgres asumido:** Docker está instalado y arranca. Si no, F2 no puede ejecutarse — bloquear y avisar.
- **F1 asumido completo:** repo git existe, package.json raíz, pnpm-workspace.yaml, stub `packages/db/package.json` con `{ name: "@simpletpv/db", private: true }`.

---

## File Structure

| Path                                                       | Acción                                             | Tarea |
| ---------------------------------------------------------- | -------------------------------------------------- | ----- |
| `.gitignore`                                               | Modificar (añadir `packages/db/generated/`)        | T1    |
| `docker-compose.yml`                                       | Crear                                              | T2    |
| `.env.example`                                             | Crear                                              | T2    |
| `packages/db/tsconfig.json`                                | Crear                                              | T3    |
| `packages/db/package.json`                                 | Modificar (deps, scripts, prisma.seed, main/types) | T3    |
| `packages/db/prisma/schema.prisma`                         | Crear                                              | T4    |
| `packages/db/prisma/migrations/<ts>_initial/migration.sql` | Autogenerada (Prisma)                              | T5    |
| `packages/db/prisma/migrations/<ts>_add_rls/migration.sql` | Crear manual                                       | T6    |
| `packages/db/prisma/seed.ts`                               | Crear                                              | T8    |

---

## Task 1: Excluir cliente Prisma generado en `.gitignore`

**Files:**

- Modify: `/Users/admin/Desktop/simpletpv/.gitignore`

- [ ] **Step 1: Verificar estado actual del `.gitignore`**

Run: `cd /Users/admin/Desktop/simpletpv && grep -n "generated" .gitignore`
Expected: muestra alguna línea relacionada con `**/generated/` (de F1) o nada.

- [ ] **Step 2: Añadir entrada específica para `packages/db/generated/`**

Editar `.gitignore` añadiendo al final, en su propia sección:

```

# Prisma generated client (packages/db)
packages/db/generated/
```

Si ya existe `**/generated/` de F1, la entrada nueva es redundante pero explícita y aceptable. No eliminar la anterior.

- [ ] **Step 3: Verificar que git ignora el path**

Run:

```bash
mkdir -p packages/db/generated && touch packages/db/generated/index.js
git check-ignore -v packages/db/generated/index.js
rm -rf packages/db/generated
```

Expected: `git check-ignore` imprime una línea con `.gitignore:<lineno>:<pattern> packages/db/generated/index.js`.

- [ ] **Step 4: Commit**

```bash
git add .gitignore
git commit -m "chore: ignorar packages/db/generated (cliente Prisma)"
```

---

## Task 2: docker-compose.yml + .env.example

**Files:**

- Create: `/Users/admin/Desktop/simpletpv/docker-compose.yml`
- Create: `/Users/admin/Desktop/simpletpv/.env.example`

- [ ] **Step 1: Crear `docker-compose.yml`**

Contenido exacto:

```yaml
services:
  postgres:
    image: postgres:16-alpine
    container_name: simpletpv-postgres
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-simpletpv}
    ports:
      - '127.0.0.1:5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-postgres}']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
```

- [ ] **Step 2: Crear `.env.example`**

Contenido exacto:

```
# Postgres local (docker compose up -d postgres)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=simpletpv

# Prisma usa esta — se conecta como superuser para migraciones y seed.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/simpletpv?schema=public

# URL del rol aplicación (la usará el API en F3, sin password porque
# será authentication peer/trust en local y password real en producción).
# DATABASE_URL_APP=postgresql://app:app@localhost:5432/simpletpv?schema=public
```

- [ ] **Step 3: Validar YAML**

Run: `python3 -c "import yaml; yaml.safe_load(open('docker-compose.yml'))" && echo OK`
Expected: `OK`.

- [ ] **Step 4: Levantar Postgres y esperar healthy**

Run:

```bash
docker compose up -d postgres
for i in $(seq 1 20); do
  status=$(docker inspect simpletpv-postgres --format '{{.State.Health.Status}}' 2>/dev/null)
  echo "postgres health: ${status:-unknown} (${i}/20)"
  [ "$status" = "healthy" ] && break
  sleep 2
done
docker compose ps postgres
```

Expected: el último `docker compose ps` muestra `simpletpv-postgres` con estado `Up ... (healthy)`.

> **Si Docker no está corriendo:** abrir Docker Desktop / iniciar el daemon y reintentar. Sin Postgres healthy, las tareas siguientes fallan.

- [ ] **Step 5: Crear `.env` local desde el example**

Run: `cp .env.example .env`

Confirmar que `.env` está en `.gitignore` (debería estarlo desde F1):

```bash
git check-ignore -v .env
```

Expected: muestra el patrón que lo ignora.

- [ ] **Step 6: Probar conexión cruda**

Run: `docker compose exec -T postgres psql -U postgres -d simpletpv -c '\dt'`
Expected: `Did not find any relations.` (DB existe pero sin tablas todavía).

- [ ] **Step 7: Commit**

```bash
git add docker-compose.yml .env.example
git commit -m "feat(db): docker-compose con postgres 16 + .env.example"
```

---

## Task 3: Actualizar `packages/db/package.json` y crear `tsconfig.json`

**Files:**

- Modify: `/Users/admin/Desktop/simpletpv/packages/db/package.json`
- Create: `/Users/admin/Desktop/simpletpv/packages/db/tsconfig.json`

- [ ] **Step 1: Sobrescribir `packages/db/package.json`**

Contenido exacto (reemplaza el stub de F1):

```json
{
  "name": "@simpletpv/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./generated/client/index.js",
  "types": "./generated/client/index.d.ts",
  "scripts": {
    "build": "prisma generate",
    "typecheck": "tsc --noEmit",
    "db:migrate": "prisma migrate dev",
    "db:migrate:deploy": "prisma migrate deploy",
    "db:reset": "prisma migrate reset --force",
    "db:studio": "prisma studio",
    "db:seed": "prisma db seed"
  },
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "bcryptjs": "^3.0.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.0",
    "prisma": "^6.0.0",
    "tsx": "^4.0.0",
    "typescript": "^6.0.0"
  }
}
```

- [ ] **Step 2: Crear `packages/db/tsconfig.json`**

Contenido exacto:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./prisma"
  },
  "include": ["prisma/**/*.ts"],
  "exclude": ["generated", "dist", "node_modules"]
}
```

- [ ] **Step 3: Validar JSON de ambos**

Run:

```bash
node -e "JSON.parse(require('fs').readFileSync('packages/db/package.json','utf8'))" && \
node -e "JSON.parse(require('fs').readFileSync('packages/db/tsconfig.json','utf8'))" && \
echo OK
```

Expected: `OK`.

- [ ] **Step 4: Instalar dependencias**

Run: `pnpm install`
Expected: sin warnings de strict-peer. `@simpletpv/db` ahora resuelve sus deps.

> **Si `@types/bcryptjs` da peer warning:** es porque `bcryptjs@3` ya trae sus tipos. Eliminar `@types/bcryptjs` del package.json y reinstalar. Continuar con el plan.

- [ ] **Step 5: Commit**

```bash
git add packages/db/package.json packages/db/tsconfig.json pnpm-lock.yaml
git commit -m "feat(db): manifest + tsconfig de @simpletpv/db (prisma + bcryptjs + tsx)"
```

---

## Task 4: Schema Prisma (sin migrar todavía)

**Files:**

- Create: `/Users/admin/Desktop/simpletpv/packages/db/prisma/schema.prisma`

- [ ] **Step 1: Crear carpeta `prisma/`**

Run: `mkdir -p packages/db/prisma`

- [ ] **Step 2: Crear `schema.prisma`**

Contenido exacto:

```prisma
generator client {
  provider = "prisma-client-js"
  output   = "../generated/client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Organization {
  id        String   @id @default(uuid()) @db.Uuid
  name      String
  nif       String?  @unique
  country   String   @default("ES")
  locale    String   @default("es-ES")
  currency  String   @default("EUR")
  createdAt DateTime @default(now())

  stores   Store[]
  users    User[]
  products Product[]
}

model Store {
  id             String   @id @default(uuid()) @db.Uuid
  organizationId String   @db.Uuid
  name           String
  address        String?
  active         Boolean  @default(true)
  createdAt      DateTime @default(now())

  organization Organization @relation(fields: [organizationId], references: [id])
  users        UserStore[]

  @@index([organizationId])
}

model User {
  id             String   @id @default(uuid()) @db.Uuid
  organizationId String   @db.Uuid
  email          String   @unique
  name           String
  passwordHash   String
  role           UserRole
  active         Boolean  @default(true)
  createdAt      DateTime @default(now())

  organization Organization @relation(fields: [organizationId], references: [id])
  stores       UserStore[]

  @@index([organizationId])
}

model UserStore {
  userId  String @db.Uuid
  storeId String @db.Uuid

  user  User  @relation(fields: [userId], references: [id])
  store Store @relation(fields: [storeId], references: [id])

  @@id([userId, storeId])
}

model Product {
  id             String   @id @default(uuid()) @db.Uuid
  organizationId String   @db.Uuid
  name           String
  salePrice      Decimal  @db.Decimal(10, 4)
  active         Boolean  @default(true)
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  organization Organization @relation(fields: [organizationId], references: [id])

  @@index([organizationId, active])
}

enum UserRole {
  ADMIN
  MANAGER
  CLERK
}
```

- [ ] **Step 3: Validar sintaxis Prisma**

Run: `pnpm --filter @simpletpv/db exec prisma format --schema=prisma/schema.prisma`
Expected: imprime el schema reformateado (sin errores). Si hay cambios cosméticos, los acepta.

- [ ] **Step 4: Commit**

```bash
git add packages/db/prisma/schema.prisma
git commit -m "feat(db): schema prisma mínimo (org/store/user/userstore/product)"
```

---

## Task 5: Migración initial (autogenerada)

**Files:**

- Create: `packages/db/prisma/migrations/<timestamp>_initial/migration.sql` (autogenerado)
- Create: `packages/db/prisma/migrations/migration_lock.toml` (autogenerado)

- [ ] **Step 1: Cargar variables de entorno**

Run: `set -a && source .env && set +a && echo "DATABASE_URL=${DATABASE_URL}"`
Expected: imprime `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/simpletpv?schema=public`.

> **Cada vez que abras una shell nueva** repite este step antes de invocar prisma — Prisma lee `DATABASE_URL` del entorno, no del `.env` salvo que se le diga explícitamente (`packages/db` no tiene dotenv config aún).

- [ ] **Step 2: Generar migración initial y aplicarla**

Run:

```bash
pnpm --filter @simpletpv/db exec prisma migrate dev --name initial --skip-seed
```

Expected: salida que incluye

```
Applying migration `<timestamp>_initial`
Your database is now in sync with your schema.
```

Y se ha generado el cliente Prisma en `packages/db/generated/client/`.

> **Si Prisma reporta "already in sync"** porque hubo un intento previo: ejecutar `pnpm --filter @simpletpv/db exec prisma migrate reset --force --skip-seed` y reintentar.

- [ ] **Step 3: Verificar que la migración existe en disco**

Run: `ls packages/db/prisma/migrations/`
Expected: una carpeta con nombre `<timestamp>_initial/` y un archivo `migration_lock.toml`.

- [ ] **Step 4: Verificar tablas en Postgres**

Run: `docker compose exec -T postgres psql -U postgres -d simpletpv -c '\dt'`
Expected: lista que incluye `Organization`, `Store`, `User`, `UserStore`, `Product`.

- [ ] **Step 5: Commit**

```bash
git add packages/db/prisma/migrations/
git commit -m "feat(db): migración initial (5 tablas)"
```

---

## Task 6: Migración add_rls (manual)

**Files:**

- Create: `packages/db/prisma/migrations/<timestamp>_add_rls/migration.sql`

- [ ] **Step 1: Generar timestamp y crear directorio**

Run:

```bash
TS=$(date -u +%Y%m%d%H%M%S)
echo "Timestamp: $TS"
mkdir -p "packages/db/prisma/migrations/${TS}_add_rls"
echo "$TS" > /tmp/simpletpv-add-rls-ts
ls "packages/db/prisma/migrations/${TS}_add_rls"
```

Expected: imprime el timestamp (formato `YYYYMMDDHHMMSS`) y crea la carpeta vacía. El timestamp queda anotado en `/tmp/simpletpv-add-rls-ts` para uso en steps siguientes.

> **Importante:** el timestamp DEBE ser posterior al de `_initial` para que Prisma respete el orden. Como `_initial` se creó hace un momento, un timestamp actual lo cumple por construcción.

- [ ] **Step 2: Crear `migration.sql` con RLS**

Crear el archivo `packages/db/prisma/migrations/${TS}_add_rls/migration.sql` (sustituir `${TS}` por el timestamp del step 1) con contenido exacto:

```sql
-- Aislamiento multi-tenant vía Row-Level Security.
-- Prisma no expresa RLS; esta migración la añade a mano sobre las tablas
-- creadas por la migración initial.

-- Roles
--   postgres  : superuser (usado por DATABASE_URL para migraciones)
--   app_admin : BYPASSRLS (usado por seed y operaciones de admin)
--   app       : RLS aplicada (usado por el API en runtime)
CREATE ROLE app NOLOGIN;
CREATE ROLE app_admin NOLOGIN BYPASSRLS;

GRANT USAGE ON SCHEMA public TO app, app_admin;
GRANT ALL ON ALL TABLES    IN SCHEMA public TO app, app_admin;
GRANT ALL ON ALL SEQUENCES IN SCHEMA public TO app, app_admin;
ALTER DEFAULT PRIVILEGES IN SCHEMA public
  GRANT ALL ON TABLES    TO app, app_admin;
ALTER DEFAULT PRIVILEGES IN SCHEMA public
  GRANT ALL ON SEQUENCES TO app, app_admin;

-- Habilitar y forzar RLS. FORCE evita que el owner escape RLS.
ALTER TABLE "Organization" ENABLE ROW LEVEL SECURITY;
ALTER TABLE "Organization" FORCE  ROW LEVEL SECURITY;
ALTER TABLE "Store"        ENABLE ROW LEVEL SECURITY;
ALTER TABLE "Store"        FORCE  ROW LEVEL SECURITY;
ALTER TABLE "User"         ENABLE ROW LEVEL SECURITY;
ALTER TABLE "User"         FORCE  ROW LEVEL SECURITY;
ALTER TABLE "Product"      ENABLE ROW LEVEL SECURITY;
ALTER TABLE "Product"      FORCE  ROW LEVEL SECURITY;

-- Política única por tabla. Si app.current_organization_id no está set,
-- current_setting(..., true) devuelve NULL → filtra a 0 filas (fail-safe).
CREATE POLICY tenant_isolation ON "Organization"
  USING (id = current_setting('app.current_organization_id', true)::uuid);

CREATE POLICY tenant_isolation ON "Store"
  USING ("organizationId" = current_setting('app.current_organization_id', true)::uuid);

CREATE POLICY tenant_isolation ON "User"
  USING ("organizationId" = current_setting('app.current_organization_id', true)::uuid);

CREATE POLICY tenant_isolation ON "Product"
  USING ("organizationId" = current_setting('app.current_organization_id', true)::uuid);

-- UserStore se filtra por join a User; sin política directa hasta que un
-- caso real lo exija.
```

- [ ] **Step 3: Aplicar la nueva migración**

Run:

```bash
set -a && source .env && set +a
pnpm --filter @simpletpv/db exec prisma migrate dev --skip-seed
```

Expected: salida incluye `Applying migration '<timestamp>_add_rls'` y termina con `Your database is now in sync with your schema.`

> **Si Prisma propone hacer un reset del schema:** algo en la migración SQL la hace incompatible (por ejemplo, un nombre de tabla mal escrito). Revisar `migration.sql` contra los nombres reales (`docker compose exec -T postgres psql -U postgres -d simpletpv -c '\dt'`) y reintentar. NO aceptar el reset hasta confirmar que es lo correcto.

- [ ] **Step 4: Verificar que RLS está activa**

Run:

```bash
docker compose exec -T postgres psql -U postgres -d simpletpv -c "
  SELECT tablename, rowsecurity, forcerowsecurity
  FROM pg_tables
  WHERE schemaname = 'public' AND tablename IN ('Organization','Store','User','Product')
  ORDER BY tablename;
"
```

Expected: 4 filas, todas con `rowsecurity = t` y `forcerowsecurity = t`.

- [ ] **Step 5: Verificar que las 4 policies existen**

Run:

```bash
docker compose exec -T postgres psql -U postgres -d simpletpv -c "
  SELECT polrelid::regclass::text AS table, polname
  FROM pg_policy
  WHERE polname = 'tenant_isolation'
  ORDER BY table;
"
```

Expected: 4 filas (Organization, Product, Store, User).

- [ ] **Step 6: Verificar que los roles existen**

Run:

```bash
docker compose exec -T postgres psql -U postgres -d simpletpv -c "
  SELECT rolname, rolbypassrls
  FROM pg_roles
  WHERE rolname IN ('app', 'app_admin')
  ORDER BY rolname;
"
```

Expected: 2 filas. `app` con `rolbypassrls = f`, `app_admin` con `rolbypassrls = t`.

- [ ] **Step 7: Commit**

```bash
git add packages/db/prisma/migrations/
git commit -m "feat(db): rls multi-tenant (roles app/app_admin + policies por tabla)"
```

---

## Task 7: Verificación cruzada — RLS realmente filtra

**Files:** ninguno permanente; solo verificación SQL.

Esta tarea no crea archivos, valida que la RLS funciona como esperado antes de seed. Si RLS está rota, el seed tampoco lo arregla.

- [ ] **Step 1: Insertar una fila de prueba como superuser**

Run:

```bash
docker compose exec -T postgres psql -U postgres -d simpletpv -c "
  INSERT INTO \"Organization\" (id, name, nif)
  VALUES ('aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa', 'TestOrgRLS', 'X999');
  SELECT COUNT(*) FROM \"Organization\";
"
```

Expected: `INSERT 0 1` y `count = 1` (superuser bypassa el FORCE RLS porque es superuser, no owner; en realidad `BYPASSRLS` aplica al rol postgres por ser superuser).

> **Nota:** el rol `postgres` siempre bypassa RLS por ser superuser. La prueba real es ejecutar como un rol no-superuser.

- [ ] **Step 2: Como rol `app` SIN setting → debe ver 0 filas**

Run:

```bash
docker compose exec -T postgres psql -U postgres -d simpletpv -c "
  SET ROLE app;
  SELECT COUNT(*) FROM \"Organization\";
  RESET ROLE;
"
```

Expected: `count = 0` (la policy con `current_setting('app.current_organization_id', true)` resuelve NULL → ninguna fila visible).

- [ ] **Step 3: Como rol `app` CON setting correcto → debe ver 1 fila**

Run:

```bash
docker compose exec -T postgres psql -U postgres -d simpletpv -c "
  SET ROLE app;
  SET LOCAL app.current_organization_id = 'aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa';
  SELECT COUNT(*) FROM \"Organization\";
  RESET ROLE;
"
```

Expected: `count = 1`.

> **Si `SET LOCAL` falla** con `ERROR: SET LOCAL can only be used in transaction blocks`: envolver con `BEGIN; ... COMMIT;`. La sintaxis exacta:
>
> ```sql
> BEGIN;
> SET ROLE app;
> SET LOCAL app.current_organization_id = 'aaaaaaaa-...';
> SELECT COUNT(*) FROM "Organization";
> COMMIT;
> ```

- [ ] **Step 4: Como rol `app_admin` → debe ver 1 fila (BYPASSRLS)**

Run:

```bash
docker compose exec -T postgres psql -U postgres -d simpletpv -c "
  SET ROLE app_admin;
  SELECT COUNT(*) FROM \"Organization\";
  RESET ROLE;
"
```

Expected: `count = 1` (app_admin tiene BYPASSRLS).

- [ ] **Step 5: Limpiar la fila de prueba**

Run:

```bash
docker compose exec -T postgres psql -U postgres -d simpletpv -c "
  DELETE FROM \"Organization\" WHERE id = 'aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa';
"
```

Expected: `DELETE 1`.

- [ ] **Step 6: Sin commit (esta tarea no toca archivos)**

No hay archivos que añadir. Si los 4 checks pasaron, RLS está sana y podemos pasar al seed.

---

## Task 8: Seed multi-tenant

**Files:**

- Create: `/Users/admin/Desktop/simpletpv/packages/db/prisma/seed.ts`

- [ ] **Step 1: Crear `seed.ts`**

Contenido exacto:

```ts
// Seed idempotente: 2 organizaciones, 2 stores cada una, 3 users por org,
// 5 products por org. Usa upsert para que correr 2 veces no duplique.
// Corre asumiendo conexión como superuser (DATABASE_URL del .env).

import bcrypt from 'bcryptjs';

import { PrismaClient, UserRole } from '../generated/client';

const prisma = new PrismaClient();

interface OrgSeed {
  nif: string;
  name: string;
  stores: Array<{ id: string; name: string }>;
  users: Array<{ email: string; name: string; role: UserRole }>;
  products: Array<{ name: string; salePrice: number }>;
}

const ORG1: OrgSeed = {
  nif: 'B11111111',
  name: 'Cadena CBD Norte',
  stores: [
    { id: '11111111-1111-1111-1111-111111111111', name: 'Tienda Madrid Centro' },
    { id: '11111111-1111-1111-1111-111111111112', name: 'Almacén Central Madrid' },
  ],
  users: [
    { email: 'admin@org1.test', name: 'Admin Org1', role: UserRole.ADMIN },
    { email: 'manager@org1.test', name: 'Manager Org1', role: UserRole.MANAGER },
    { email: 'clerk@org1.test', name: 'Clerk Org1', role: UserRole.CLERK },
  ],
  products: [
    { name: 'Flor CBD 20%', salePrice: 12.5 },
    { name: 'Aceite CBD 5%', salePrice: 29.9 },
    { name: 'Crema CBD', salePrice: 19.95 },
    { name: 'Té CBD', salePrice: 7.5 },
    { name: 'Vape CBD', salePrice: 34.0 },
  ],
};

const ORG2: OrgSeed = {
  nif: 'B22222222',
  name: 'Distribuidora Sur',
  stores: [
    { id: '22222222-2222-2222-2222-222222222221', name: 'Tienda Sevilla' },
    { id: '22222222-2222-2222-2222-222222222222', name: 'Tienda Málaga' },
  ],
  users: [
    { email: 'admin@org2.test', name: 'Admin Org2', role: UserRole.ADMIN },
    { email: 'manager@org2.test', name: 'Manager Org2', role: UserRole.MANAGER },
    { email: 'clerk@org2.test', name: 'Clerk Org2', role: UserRole.CLERK },
  ],
  products: [
    { name: 'Bolsa premium 1g', salePrice: 9.0 },
    { name: 'Pack mensual', salePrice: 49.0 },
    { name: 'Accesorio A', salePrice: 4.5 },
    { name: 'Accesorio B', salePrice: 6.0 },
    { name: 'Merch camiseta', salePrice: 18.0 },
  ],
};

async function seedOrg(spec: OrgSeed, passwordHash: string): Promise<void> {
  const org = await prisma.organization.upsert({
    where: { nif: spec.nif },
    update: {},
    create: { name: spec.name, nif: spec.nif },
  });

  for (const store of spec.stores) {
    await prisma.store.upsert({
      where: { id: store.id },
      update: {},
      create: { id: store.id, organizationId: org.id, name: store.name },
    });
  }

  for (const user of spec.users) {
    await prisma.user.upsert({
      where: { email: user.email },
      update: {},
      create: {
        organizationId: org.id,
        email: user.email,
        name: user.name,
        passwordHash,
        role: user.role,
      },
    });
  }

  for (const p of spec.products) {
    const existing = await prisma.product.findFirst({
      where: { organizationId: org.id, name: p.name },
    });
    if (!existing) {
      await prisma.product.create({
        data: {
          organizationId: org.id,
          name: p.name,
          salePrice: p.salePrice,
        },
      });
    }
  }
}

async function main(): Promise<void> {
  const passwordHash = await bcrypt.hash('password123', 10);
  await seedOrg(ORG1, passwordHash);
  await seedOrg(ORG2, passwordHash);
  console.log('Seed completado: 2 organizaciones.');
}

main()
  .catch((err) => {
    console.error(err);
    process.exitCode = 1;
  })
  .finally(() => prisma.$disconnect());
```

- [ ] **Step 2: Verificar typecheck del seed**

Run: `pnpm --filter @simpletpv/db typecheck`
Expected: sin errores. Si Prisma no ha generado todavía el cliente, ejecutar primero `pnpm --filter @simpletpv/db exec prisma generate` y reintentar.

> **Si tsc se queja de `Cannot find module '../generated/client'`:** el cliente no se generó. Ejecutar `pnpm --filter @simpletpv/db exec prisma generate` y volver a typecheck.

- [ ] **Step 3: Ejecutar seed**

Run:

```bash
set -a && source .env && set +a
pnpm --filter @simpletpv/db exec prisma db seed
```

Expected: imprime

```
Running seed command `tsx prisma/seed.ts` ...
Seed completado: 2 organizaciones.
```

- [ ] **Step 4: Re-ejecutar seed (idempotencia)**

Run: `pnpm --filter @simpletpv/db exec prisma db seed`
Expected: mismo mensaje `Seed completado: 2 organizaciones.` Y sin duplicados.

- [ ] **Step 5: Verificar conteos en DB**

Run:

```bash
docker compose exec -T postgres psql -U postgres -d simpletpv -c "
  SELECT
    (SELECT COUNT(*) FROM \"Organization\") AS orgs,
    (SELECT COUNT(*) FROM \"Store\")        AS stores,
    (SELECT COUNT(*) FROM \"User\")         AS users,
    (SELECT COUNT(*) FROM \"Product\")      AS products;
"
```

Expected: `orgs=2, stores=4, users=6, products=10`.

- [ ] **Step 6: Verificar que dos invocaciones no duplican**

Run: `pnpm --filter @simpletpv/db exec prisma db seed && docker compose exec -T postgres psql -U postgres -d simpletpv -c 'SELECT COUNT(*) FROM "Organization";'`
Expected: sigue siendo `count = 2`.

- [ ] **Step 7: Commit**

```bash
git add packages/db/prisma/seed.ts
git commit -m "feat(db): seed multi-tenant con 2 organizaciones idempotente"
```

---

## Task 9: Validación de cierre de F2

**Files:** ninguno permanente; solo verificación.

- [ ] **Step 1: Verificar los 13 checks del spec §6**

Ejecutar uno por uno, esperando los resultados indicados:

```bash
# 1. Postgres healthy
docker compose ps postgres
# Expected: simpletpv-postgres Up (healthy)

# 2-3. .env ya existe (lo creamos en T2 step 5)
ls .env && git check-ignore -v .env
# Expected: .env existe y está ignorado

# 4. install limpio
pnpm install
# Expected: sin warnings strict-peer

# 5. generate
pnpm --filter @simpletpv/db exec prisma generate
# Expected: ✔ Generated Prisma Client

# 6-7. migraciones aplicadas (ya están de T5/T6)
ls packages/db/prisma/migrations/
# Expected: 2 carpetas <ts>_initial y <ts>_add_rls + migration_lock.toml

# 8. seed corre
set -a && source .env && set +a
pnpm --filter @simpletpv/db exec prisma db seed
# Expected: Seed completado: 2 organizaciones.

# 9. seed idempotente (ya verificado en T8 step 6)

# 10. count orgs
docker compose exec -T postgres psql -U postgres -d simpletpv -tc 'SELECT COUNT(*) FROM "Organization";'
# Expected: 2

# 11. policies
docker compose exec -T postgres psql -U postgres -d simpletpv -tc "SELECT COUNT(*) FROM pg_policy WHERE polname = 'tenant_isolation';"
# Expected: 4

# 12. roles
docker compose exec -T postgres psql -U postgres -d simpletpv -tc "SELECT COUNT(*) FROM pg_roles WHERE rolname IN ('app','app_admin');"
# Expected: 2

# 13. git limpio
git status --porcelain
# Expected: vacío (sin .env porque está ignorado)
```

- [ ] **Step 2: Verificar que F1 sigue verde**

Run:

```bash
pnpm lint && pnpm format
```

Expected: ambos pasan sin error.

> **Si `pnpm format` falla por algún archivo `.sql` o `.prisma`:** añadir esos paths a `.prettierignore` (Prettier no formatea SQL ni Prisma por defecto, pero por si acaso). Documentado: schema.prisma se formatea con `prisma format`, no con Prettier.

- [ ] **Step 3: Verificar que `pnpm build` (Turborepo) pasa**

Run: `pnpm build`
Expected: Turborepo ejecuta `prisma generate` en `@simpletpv/db` (el único workspace con script `build` ahora) y termina exit 0.

- [ ] **Step 4: Confirmar git log**

Run: `git log --oneline | head -10`
Expected: al menos 7 commits nuevos de F2 (T1-T6, T8), todos Conventional Commits.

- [ ] **Step 5: Sin commit final**

T9 solo valida; no añade archivos.

---

## Self-review (ejecutado al escribir el plan)

**1. Cobertura del spec:**

| Spec §                         | Cubierto por                     |
| ------------------------------ | -------------------------------- |
| §4 estructura completa         | T1-T8 (cada archivo tiene tarea) |
| §5.1 docker-compose.yml        | T2 step 1                        |
| §5.2 .env.example              | T2 step 2                        |
| §5.3 .gitignore actualizado    | T1                               |
| §5.4 packages/db/package.json  | T3 step 1                        |
| §5.5 packages/db/tsconfig.json | T3 step 2                        |
| §5.6 schema.prisma             | T4 step 2                        |
| §5.7 migración add_rls         | T6 step 2                        |
| §5.8 seed.ts                   | T8 step 1                        |
| §6 validación 13 checks        | T9 step 1                        |
| §8 definición de done          | T9 (todos los puntos cubiertos)  |

Extra que añade el plan: T7 verifica que RLS realmente filtra antes de seed. No está en el spec pero es un sanity check útil — si RLS está rota, descubrir antes que después ahorra horas.

Sin gaps.

**2. Placeholder scan:**

- `<timestamp>` y `<ts>` en T5 y T6 son notación de Prisma (no placeholders a rellenar a mano salvo en T6 donde el plan explicita el cómo). T6 step 1 captura el timestamp en una variable shell `$TS` y la usa en step 2; el contenido SQL es íntegro. Aceptable.
- T6 step 2 instruye explícitamente "sustituir `${TS}` por el timestamp del step 1" — no es ambigüedad, es ejecutar la sustitución.
- Notas `> **Si X:** ...` son contingencias, no diferimientos.

**3. Consistencia de tipos/nombres:**

- `@simpletpv/db` consistente en T1, T3, T5-T9.
- `Organization`, `Store`, `User`, `UserStore`, `Product` consistentes entre schema (T4), migración RLS (T6), seed (T8), verificación (T9).
- `app` y `app_admin` consistentes entre T6 (creación), T7 (verificación), T9 (check 12).
- `DATABASE_URL` consistente entre `.env.example` (T2) y consumo (T5, T6, T8).
- `tenant_isolation` consistente entre T6 step 2 y T6 step 5 y T9 check 11.

Sin issues detectados.
